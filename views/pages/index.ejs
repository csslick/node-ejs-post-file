<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="container my-4 border-bottom py-3">
    <a href="/">home</a>
    <a href="/about">about</a>
  </nav>

  <div class="container">
    <h1 class="mb-3">게시판</h1>
    <form action="/create" method="post" class="mb-5">
      <div class="row mb-3">
        <div class="col">
          <textarea name="comment" id="comment" rows="3" placeholder="글을 입력하세요" class="form-control"></textarea>
        </div>
      </div>
      <div class="row mb-3">
        <div class="col">
          <input type="text" name="name" placeholder="이름" class="form-control">
        </div>
        <div class="col">
          <input type="text" name="pwd" placeholder="비밀번호" class="form-control">
        </div>
      </div>
      <button type="submit" class="btn btn-primary">글쓰기</button>
    </form>
    <section>
      <h2 class="mb-3">글목록</h2>
      <small>총게시물: <%= posts.length %></small>
      <ul class="post-list">
      <!-- 내림차순(큰번호 순, 최근 작성 글) -->
      <% for(let i = posts.length - 1; i >= 0; i--) { %>
        <li class="border-bottom py-2">
          <div>
            <span>글번호: <%= i %></span>
            <b><%= posts[i].name %></b>
            <p><%= posts[i].comment %> </p>
          </div>
          <!-- <form action="/delete/<%= i %>" method="post">
            <button type="submit" class="btn btn-danger">삭제</button>
          </form> -->
          <button 
            onclick="isAdmin('<%= posts[i].name %>', '<%= posts[i].pwd %>')" 
            class="btn btn-danger">삭제
          </button>
        </li>
      <% } %>
      </ul>
    </section>
  </div><!-- container -->

</body>
</html>